name: async_await_cancel
author: allen brunson
description: async/await cancellation demo
version: 1.0.0
publish_to: "none"

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=2.0.0 <3.0.0"
flutter:
  uses-material-design: true

dependencies:
  async: ^2.6.1        # https://pub.dev/packages/async
  flutter:
    sdk: flutter
